# Ejemplo de .env para backend (Node.js/Express)
# No incluyas datos reales ni secretos en GitHub

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=usuario_ejemplo
DB_PASSWORD=contraseña_ejemplo
DB_NAME=nombre_base_ejemplo

# Puerto del backend
PORT=3001

# JWT_SECRET (si usas JWT)
JWT_SECRET=secreto_ejemplo
